## Copy this file to `keystore.properties` (git-ignored) and fill in your own
## release signing credentials. CI uploads its values via repository secrets
## and generates this file during the build job — do NOT commit secrets.

# Path to the keystore relative to the project root.
storeFile=release.keystore

storePassword=CHANGE_ME
keyAlias=CHANGE_ME
keyPassword=CHANGE_ME
